meta {
  name: get-widget-dashboard
  type: http
  seq: 2
  # orphan: no in-repo callers found
}

get {
  url: {{baseUrl}}/api/widget/dashboard
  auth: bearer
}

auth { mode: bearer }
auth:bearer { token: {{API_KEY}} }
docs {
  Domain: widget
  Auth detected: user, api-key, role:teacher+student
  Schemas: —
}
