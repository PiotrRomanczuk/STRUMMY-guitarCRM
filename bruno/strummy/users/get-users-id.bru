meta {
  name: get-users-id
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/users/{{sample_id}}
  auth: inherit
}

docs {
  Domain: users
  Auth detected: user, role:admin+teacher+student
  Schemas: —
}
