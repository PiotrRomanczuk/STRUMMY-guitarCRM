meta {
  name: get-song-admin-favorites
  type: http
  seq: 10
  # orphan: no in-repo callers found
}

get {
  url: {{baseUrl}}/api/song/admin-favorites
  auth: inherit
}

docs {
  Domain: song
  Auth detected: user, role:admin
  Schemas: —
}
