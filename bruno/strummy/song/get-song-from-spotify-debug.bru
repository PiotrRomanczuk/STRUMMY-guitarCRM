meta {
  name: get-song-from-spotify-debug
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/song/from-spotify/debug
  auth: inherit
}

tests {
  test('Status 200 or 403', function() {
    expect([200, 403]).to.include(res.status);
  });
}