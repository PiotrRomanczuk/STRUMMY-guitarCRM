meta {
  name: delete-lessons-songs-id
  type: http
  seq: 18
  # orphan: no in-repo callers found
}

delete {
  url: {{baseUrl}}/api/lessons/songs/{{sample_id}}
  auth: inherit
}

docs {
  Domain: lessons
  Auth detected: user
  Schemas: —
}
