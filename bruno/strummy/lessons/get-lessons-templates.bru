meta {
  name: get-lessons-templates
  type: http
  seq: 24
  # orphan: no in-repo callers found
}

get {
  url: {{baseUrl}}/api/lessons/templates
  auth: inherit
}

docs {
  Domain: lessons
  Auth detected: user
  Schemas: —
}
