meta {
  name: post-admin-set-passwords
  type: http
  seq: 11
  # orphan: no in-repo callers found
}

post {
  url: {{baseUrl}}/api/admin/set-passwords
  body: json
  auth: bearer
}

auth { mode: bearer }
auth:bearer { token: {{SUPABASE_SERVICE_ROLE_KEY}} }
body { json }

body:json {
  {}
}
docs {
  Domain: admin
  Auth detected: service-role
  Schemas: —
}
